spring:
  application:
   name: spring-boot-rest-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_first_database?useTimeZone=true&serverTimeZone=UTC
    username: root
    password: MyNewPass
  jpa:
    hibernate:
     ddl-auto: update
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
      show-sql: false

      open-in-view: false
